version: '3.8'

services:
  dotmate:
    image: ghcr.io/${GITHUB_REPOSITORY:-leslieleung/dotmate}:latest
    container_name: dotmate
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./logs:/app/logs
    environment:
      - TZ=${TZ:-UTC}